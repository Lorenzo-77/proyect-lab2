<script> 
   function clickAndDisable(link) {
     // disable subsequent clicks
     link.onclick = function(event) {
        event.preventDefault();
     }
   }   
</script>

<div class="container p-4">
    <div class="row">
        {{#each materiasArr }}
             <div class="col md-3">
                 <div class="card text-center text-white  bg-secondary mb-3"> 
                     <div class="card-body">
                            <h4 class="card-title justify-content-between align-items-center">
                                 {{nombreMateria}}
                             </h4>
                         </a>
       <p class="m-2">Profesor {{nombre}} {{apellido}}</p>
      {{#each horarios}}
        <p class="m-2">{{dia}} {{hora_inicio}} {{hora_fin}}</p>   
      {{/each}}
                        {{#if (nMateria)}}
                           <a href="/materias/inscribir/{{idhorarios}}" class="btn btn-outline-light" >Inscribirse 1</a> 
                        {{/if}}
                        
                        
                         <a href="/materias/inscribir/{{idhorarios}}" class="btn btn-outline-light" >Inscribirse 2</a>

                     </div>
                 </div>
             </div>
        {{else}}  
            <div class="col-md-4 mx-auto">
                <div class="card card-body text-center">
                    <P>No hay Inscripciones Guardados.</P>
                    
                </div>
            </div>
        {{/each}}
    </div>
</div>
<!--
<div class="row">
<h3>Materia</h3>
  {{#each materia}}
    <div class="col-md-3">      
      <div class="card text-white bg-success mb-3">
        <div class="card-body">
          <h4 class="card-title d-flex justify-content-between align-items-center">
            {{nombreMateria}}  
          </h4>

           <a class="btn btn-light btn-block btn-sm" href="/materias/inscripcion/{{alumnoId}}">Registrarse</a>

        </div>
      </div>
    </div>
    {{/each}}
</div>-->