
<div class="container p-4">
    <div class="row"> 
        {{#each materiasArr }}
             <div class="col md-3">
                 <div class="card text-center text-gray-900 bg-gradient-light mb-3"> 
                     <div class="card-body">
                            <h4 class="card-title justify-content-between align-items-center">
                                 {{nombreMateria}}
                             </h4>
                         </a>
                        <p class="m-2">Profesor {{nombre}} {{apellido}}</p>
      {{#each horarios}}
        <p class="m-2">{{dia}} {{hora_inicio}} {{hora_fin}}</p>   
      {{/each}}

                        
                         <a href="/materias/inscribir/{{idhorarios}}" class="btn btn-outline-light text-gray-900" >Inscribirse</a>

                     </div>
                 </div>
             </div>
        {{else}}  
            <div class="col-md-4 mx-auto">
                <div class="card card-body text-center">
                    <P>No hay Inscripciones Guardados.</P>
                    
                </div>
            </div>
        {{/each}}
    </div>
</div>
