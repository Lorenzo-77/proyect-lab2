<div class="container p-4">
    <div class="row">
        {{#each materiasArr}}
             <div class="col md-3">
                 <div class="card text-center">
                     <div class="card-body ">
                             <h3 class="card-title text-uppercase text-info">
                                 {{nombreMateria}}
                             </h3>
                         </a>
                         
                         <p class="m-2 text-success">Profesor {{nombre}} {{apellido}}</p>
                            {{#each horarios}}
        <p class="m-2">{{dia}} {{hora_inicio}} {{hora_fin}}</p>   
      {{/each}}
                         <a href="/asistencia/agregarAsistencia/{{idhorarios}}" class="btn btn-outline-dark">Asistencia</a>
                    

                     </div>
                 </div>
             </div>
        {{else}}  
            <div class="col-md-4 mx-auto">
                <div class="card card-body text-center">
                    <P>No hay Materias Guardados.</P>
                    
                </div>
            </div>
        {{/each}}
    </div>
</div>